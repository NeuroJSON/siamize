# Build artifacts
build/
*.o
*.a

# MEX outputs dropped next to matlab/siamize.m by `make mex-matlab`,
# `make mex-octave`, `make cudamex`, and `make cudaoct`. These are
# per-host binaries; users build them locally.
matlab/siamex.mex
matlab/siamex.mexa64
matlab/siamex.mexmaca64
matlab/siamex.mexmaci64
matlab/siamex.mexw64

# Fetched dependencies (pulled by scripts/fetch_deps.sh on demand)
third_party/

# Pretrained weights (fetched via scripts/fetch_weights.sh)
models/

# Predictions generated by tests
tests/pred_*.nii.gz
!tests/pred_ref_allfolds.nii.gz

# Python
__pycache__/
*.pyc

# Conversation transcripts from `/export`
[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]-*.txt
octave-workspace
sub-01_T1w.nii.gz
output.jnii
